# Method Applications in C#

## Overview
This project demonstrates the use of basic method applications in C#. Methods can take parameters, return values, or perform operations without returning anything (void methods).
